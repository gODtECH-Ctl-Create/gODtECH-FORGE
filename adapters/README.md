# Agent Adapters

Adapters make the same FORGE system consumable by different AI coding environments.

Planned adapters:

- `codex/`
- `claude/`
- `cursor/`
- `github-copilot/`
- `generic/`

Adapters should translate conventions and entry points only. They should not duplicate the core intelligence or create vendor-specific copies of project truth.
