# Core

The core defines how FORGE selects context, activates the right intelligence, records decisions, tracks state, and hands work through the lifecycle.

Core responsibilities should stay small. Domain knowledge belongs in `../intelligence/`; enforceable checks belong in `../verification/`.
