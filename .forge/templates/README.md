# Templates

Templates are project artifacts that FORGE may generate or initialize from structured context.

Examples include project briefs, architecture records, design decisions, security assessments, verification plans, and deployment checklists.

Templates should be generic. Project-specific values belong in `../context/` and `../decisions/`.
