{
  "name": "@godtech/forge",
  "version": "0.1.0",
  "description": "A reusable operating framework for deliberate, verified AI-assisted product engineering.",
  "type": "module",
  "bin": {
    "forge": "dist/src/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc && node scripts/copy-framework.mjs",
    "check": "tsc --noEmit",
    "test": "npm run build && node scripts/run-tests.mjs",
    "prepack": "npm run build"
  },
  "keywords": [
    "ai",
    "coding-agent",
    "mcp",
    "developer-tools",
    "verification"
  ],
  "license": "UNLICENSED",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.19.11",
    "typescript": "^5.9.2"
  }
}
